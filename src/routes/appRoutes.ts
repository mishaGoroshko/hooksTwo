export enum AppRoutePaths {
  TEXTAREA = '/',
  SELECT = '/select',
  COORDS = '/coords',
  COUNTER = '/counter',
  USERS = '/users',
  STYLED = '/styled',
  RANDOM = '*',
  NOT_FOUND = '/404-not-found',
}
